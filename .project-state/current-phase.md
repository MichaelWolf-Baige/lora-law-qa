# 当前进度指针

**阶段**：第二步「做」（纠偏）—— 项目已存在且全链路跑通，本轮是「完善」，非从零立项。

**本轮四个方向（按用户勾选优先级）**：

1. ✅ 修过度拒答 —— 根因已诊断（见 anchor.md），待实现推理侧修复
2. ⬜ 工程卫生 —— git + 锁版本 + 文档对齐
3. ⬜ 修评测口径 —— 测试集重标 + NHSR 引用抽取 + 指标拆分
4. ⬜ SafeDPO 对齐 —— 准备偏好对 + 训练命令

**进度**：诊断完成 → 开始工程卫生 → 代码修复 → 交付/回退。

**回退规则**：出错回到最近 checkpoint（本目录文件），禁止重跑已完成阶段。
